% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_from_ggplot.R
\name{hc_from_ggplot}
\alias{hc_from_ggplot}
\title{Convert a ggplot2 plot to a Highcharts widget}
\usage{
hc_from_ggplot(
  p,
  title = NULL,
  subtitle = NULL,
  tooltip_shared = TRUE,
  tooltip_format = NULL,
  tooltip_formatter = NULL,
  animation = TRUE,
  warn_unsupported = TRUE,
  ...
)
}
\arguments{
\item{p}{A ggplot object.}

\item{title}{Optional character string to override the plot title.}

\item{subtitle}{Optional character string to override the plot subtitle.}

\item{tooltip_shared}{Logical; if \code{TRUE} (default), uses shared tooltip
so all series values are shown at the same x position.}

\item{tooltip_format}{Optional character string for Highcharts
\code{tooltip.pointFormat}. Overrides the default tooltip format. Use
Highcharts template variables like \code{{point.x}}, \code{{point.y}},
\code{{series.name}}, and \code{{point.text}} (for custom text from
\code{aes(text = ...)}).}

\item{tooltip_formatter}{Optional \code{\link[htmlwidgets:JS]{htmlwidgets::JS()}} function for
complete tooltip control. Takes precedence over \code{tooltip_format}. The
function receives the standard Highcharts tooltip context (\code{this.point},
\code{this.series}, etc.).}

\item{animation}{Controls chart animation. \code{TRUE} (default) enables
default Highcharts animation. \code{FALSE} disables all animation. A numeric
value sets the animation duration in milliseconds.}

\item{warn_unsupported}{Logical; if \code{TRUE} (default), emits warnings when
unsupported aesthetics or features are silently dropped.}

\item{...}{Reserved for future use.}
}
\value{
A \code{\link[highcharter:highchart]{highcharter::highchart()}} widget that can be printed,
saved, or included in Shiny apps and R Markdown documents.
}
\description{
Translates a restricted subset of ggplot2 plots into interactive
highcharter widgets. Works by inspecting the built plot
(via \code{\link[ggplot2:ggplot_build]{ggplot2::ggplot_build()}}) rather than parsing source code.
}
\section{Supported chart types}{

\itemize{
\item \strong{Line charts}: \code{geom_line()}, \code{geom_step()}
\item \strong{Scatter plots}: \code{geom_point()}
\item \strong{Bar/Column charts}: \code{geom_col()}, \code{geom_bar()}, \code{geom_histogram()}
\item \strong{Area charts}: \code{geom_area()}, \code{geom_ribbon()}
\item \strong{Statistical}: \code{geom_smooth()}, \code{geom_boxplot()}, \code{geom_errorbar()}
\item \strong{Reference lines}: \code{geom_hline()}, \code{geom_vline()}
}
}

\section{Supported axes}{

\itemize{
\item X axis: discrete (categorical), numeric, Date, POSIXct (datetime)
\item Y axis: numeric (continuous)
}
}

\section{Unsupported features (will error)}{

\itemize{
\item Facets (\code{facet_wrap()}, \code{facet_grid()})
\item Non-cartesian coordinates (\code{coord_polar()}, \code{coord_sf()}, etc.)
\item Unsupported geoms (\code{geom_text()}, \code{geom_tile()},
\code{geom_violin()}, \code{geom_density()}, etc.)
}
}

\examples{
\dontrun{
library(ggplot2)
library(gghighcharts)

# Simple scatter plot
p <- ggplot(mtcars, aes(wt, mpg)) + geom_point()
hc_from_ggplot(p)

# Multi-series line chart
p <- ggplot(economics_long, aes(date, value01, colour = variable)) +
  geom_line()
hc_from_ggplot(p, title = "US Economic Indicators")

# Bar chart
p <- ggplot(mpg, aes(class, fill = class)) +
  geom_bar(stat = "count")
# Note: stat = "count" is NOT supported; use pre-aggregated data:
df <- as.data.frame(table(mpg$class))
p <- ggplot(df, aes(Var1, Freq)) + geom_col()
hc_from_ggplot(p)
}

}
